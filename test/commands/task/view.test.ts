import {expect, test} from '@oclif/test'

describe('view', () => {
  // test
  // .stdout()
  // .command(['view', '--help'])
  // .it('shows help', ctx => {
  //   expect(ctx.stdout).to.contain('help')
  // });
})
